name: StatsService Docker Build & Push

on:
  push:
    branches:
      - dev_asmitha_deployment_voteservice
    paths:
      - "StatsService.Api/**"

jobs:
  build-and-push:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME_ASMITHA }}
          password: ${{ secrets.DOCKER_PASSWORD_ASMITHA }}

      - name: Build and Push StatsService
        uses: docker/build-push-action@v5
        with:
          context: ./StatsService.Api
          dockerfile: ./StatsService.Api/Dockerfile
          push: true
          tags: |
            ${{ secrets.DOCKER_USERNAME_ASMITHA }}/statsservice:latest
            ${{ secrets.DOCKER_USERNAME_ASMITHA }}/statsservice:${{ github.sha }}
